export interface IColorProps {
  brand: {
    primary: string;
  };
  bg: {
    primary: string;
    secondary: string;
  };
  text: {
    primary: string;
    secondary: string;
  };
  divider: {
    primary: string;
  };
  helpers: {
    error: string;
  };
  icons: {
    primary: string;
  };
}
